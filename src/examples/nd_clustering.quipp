
let dim = 10N;
let nclusters = 3N;
let pointType = iterate dim (TPair TDouble) TUnit;
let clusterType = iterate nClusters TMaybe TVoid;

let getPoint = randFunction clusterType pointType;
\_ ->
let cluster = uniform clusterType;
(cluster, getPoint getCluster)

