let numComponents = 3N;
let pointDim = 5N;
let getPoint = randFunction (TTuple numComponents TDouble) (TTuple pointDim TDouble);

\un ->
let x = replicate numComponents standardNormal;
(x, getPoint x)
